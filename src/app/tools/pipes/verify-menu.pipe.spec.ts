import { VerifyMenuPipe } from './verify-menu.pipe';

describe('VerifyMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new VerifyMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
