import { InstitutionPosition } from './institution-position';

describe('InstitutionPosition', () => {
  it('should create an instance', () => {
    expect(new InstitutionPosition()).toBeTruthy();
  });
});
