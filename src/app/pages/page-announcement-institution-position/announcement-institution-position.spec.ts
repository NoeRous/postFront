import { AnnouncementInstitutionPosition } from './announcement-institution-position';

describe('AnnouncementInstitutionPosition', () => {
  it('should create an instance', () => {
    expect(new AnnouncementInstitutionPosition()).toBeTruthy();
  });
});
