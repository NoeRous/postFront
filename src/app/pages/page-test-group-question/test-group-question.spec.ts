import { TestGroupQuestion } from './test-group-question';

describe('TestGroupQuestion', () => {
  it('should create an instance', () => {
    expect(new TestGroupQuestion()).toBeTruthy();
  });
});
